{{!-- navbar --}}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="https://github.com/MR-Stan/Look-Out">Look Out Logo</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                {{!-- change location --}}
                <form class="clearfix mb-4" method="POST" action="/location">
                    <button id="button" class="btn btn-primary navbar-btn">Change Location</button>
                </form>
            </li>
            <li class="nav-item">
                {{!-- access favorites --}}
                <form class="clearfix mb-4" method="GET" action="/favorites">
                    <button id="button" class="btn btn-primary navbar-btn">Favorites</button>
                </form>
            </li>
            <li class="nav-item">
                {{!-- add favorite --}}
                <form class="clearfix mb-4" method="POST" action="/favorites/add">
                    <button id="button" class="btn btn-primary navbar-btn">Add Favorite</button>
                </form>
            </li>
            <li class="nav-item">
                {{!-- logout --}}
                <form class="clearfix mb-4" method="POST" action="/logout">
                    <button id="button" class="btn btn-primary navbar-btn">Logout</button>
                </form>
            </li>
        </ul>
    </div>
</nav>